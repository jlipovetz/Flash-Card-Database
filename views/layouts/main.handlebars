<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Flashcard Database</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
  <link href="/css/style.css" rel="stylesheet" />
</head>

<body>
  <div>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark" style="font-family: audiowide; color: #0F0F0F; background: linear-gradient(0deg, rgb(174, 176, 174) 0%, rgb(17, 56, 15) 100%)">
      <div class="container-fluid">
        <h1>Flashcard Database for Students</h1>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#dark-nav" area-controls="darknav" area-expanded="false" area-label="toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="row justify-content-end">
          <div class="collapse navbar-collapse" id="dark-nav">
            <ul class="navbar-nav">
              <li class="nav-item active">
                {{#if loggedIn}}
                <a class="nav-link" onmouseover="this.style.backgroundColor='#8347FA'" onmouseout="this.style.backgroundColor=''" href="/deck-edit" data-bs-toggle="modal" data-bs-target="#pet-finder-modal">
                  <h5>Create a deck</h5>
                </a>
                {{else}}
                <a class="nav-link" onmouseover="this.style.backgroundColor='#8347FA'" onmouseout="this.style.backgroundColor=''" href="/login" data-bs-toggle="modal" data-bs-target="#pet-finder-modal">
                  <h5>Create a deck</h5>
                </a>
                {{/if}}
              </li>
            </ul>
          </div>
        </div>
        <div class="row justify-content-end">
          <div class="collapse navbar-collapse" id="dark-nav">
            <ul class="navbar-nav">
              <li class="nav-item active">
                {{#if loggedIn}}
                <button id="logout" class="btn btn-danger">Logout</button>
                {{else}}
                <a class="nav-link" onmouseover="this.style.backgroundColor='#8347FA'" onmouseout="this.style.backgroundColor=''" href="/login" data-bs-toggle="modal" data-bs-target="#pet-finder-modal">
                  <h5>Login </h5>
                </a>
                {{/if}}
              </li>
            </ul>
          </div>
        </div>
        <div class="row justify-content-end">
          <div class="collapse navbar-collapse" id="dark-nav">
            <ul class="navbar-nav">
              <li class="nav-item active">
                <a class="nav-link" onmouseover="this.style.backgroundColor='#8347FA'" onmouseout="this.style.backgroundColor=''" href="/signup" data-bs-toggle="modal" data-bs-target="">
                  <h5>Sign up</h5>
                </a>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </nav>
    <div class="body">
      <div class="decksection">
        {{{body}}}
      </div>
    </div>
  </div>

  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>